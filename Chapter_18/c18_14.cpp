// For data could be held in one machine, we use external sort: divide the data into several file, sort each file and merge all files with heap.
// For data could not be held in one machine, we use one machine to handle a range of data.
