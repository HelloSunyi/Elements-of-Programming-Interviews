// 1. The easist way to merge is sorted linked list.
// 2. So, we need to transfer BST to linked list firstly,
// it is eazy to transfer from BST to sorted doubly linked list(c14_7).
// 3. Then merge two doubled linked list.
// 4. Generate BST from sorted doubly linked list(c14_8).
