// For reading the last n lines, use a n lines sized queue, each time read a time and push into the end of queue;
// When the queue is full, for a new comming line, delete the last line in the end of queue, untill finish reading the file
