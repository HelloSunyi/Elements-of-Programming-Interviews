// List the most popular and latest articles.
// Classify the articles: accroding to the keywords/tags into subset; users could add tags. Provide similar articles in the subset.
// System find the similar articles by matching same words: ignore words like: is are and. Give high weight to some significant words
// Analyze server log, if many users read article A and B in a time interval, so when user read acticle A, recommend B
