// The solution is similar with insertion sort:
// For the biggest one, move it to the top position(one flip) and then move to the bottom(one flip, totally two flips).
// Then the second biggest one, and etc...
// The time complexity is O(n2)
