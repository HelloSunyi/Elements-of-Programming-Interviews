// Classical divide and conque problem
// Sort according to x axis, didive into two parts by x = p
// For each part, find the closest distance d1, d2, d = min(d1, d2);
// Collect points which x [p-d, p+d], for these points, find closest distance by sorting y axis.
