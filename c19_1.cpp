// Divde the target image into s*s sized squres, and compute the average color the squre
// Compute the average color of each tile/image.
// For each squre, find the closest matched image, O(nm) time complexity.
// Optimization: compute in parallel, use search tree.
